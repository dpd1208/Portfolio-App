import React, { useEffect } from 'react';
import PropTypes from 'prop-types';
import { SectionWrapper } from './Animation.styled';

const ProfilePhoto = ({ testId, className }) => {
	useEffect(() => {}, []);

    return ();
    
    export default ProfilePhoto;